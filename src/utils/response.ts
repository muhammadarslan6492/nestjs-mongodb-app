export enum ResponseMessage {
  USER_SIGNUP = 'User signup successfully',
}

export enum ErrorMessage {
  USER_ALREADY_EXIST = 'User already exist in the system',
  INVALID_CREDENTIAL = 'Email OR Password Invalid',
}
